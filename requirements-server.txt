# Core dependencies (main server machine)
httpx>=0.27.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.0.0

# Google Calendar integration
google-api-python-client>=2.150.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Twilio (calls & SMS)
twilio>=9.0.0

# Knowledge Base (local vector DB)
chromadb>=0.5.0

# Code Generation (no extra deps - uses Claude API via httpx)

# Spotify Music (uses httpx — no extra deps, just needs SPOTIFY_CLIENT_ID/SECRET in .env)

# Smart Home (Home Assistant REST API — uses httpx, no extra deps)
# For MQTT support (optional): paho-mqtt>=2.0.0

# Scheduler (no external deps — lightweight built-in scheduler)

# Web Search (free DuckDuckGo search, no API key needed)
duckduckgo-search>=6.0.0

# Timezone support for Mel's system prompt
pytz>=2024.0
